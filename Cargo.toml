[package]
name = "cargo-gen-helpers"
version = "0.0.1"
authors = ["Linas Juškevičius <linas@prodigito.lt>"]

[dependencies]
askama = "0.5"
error-chain = "0.11"
cargo = "0.22"
tempdir = "0.3"

[dev-dependencies]
tempfile = "2.2"

[build-dependencies]
askama = "0.5"
